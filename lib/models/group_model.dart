import 'package:flutter/cupertino.dart';

class GroupModel {
  //this will  need changing when people become classes, strings for temporary
  //also add icons
  List<String> members;
  Icon icon;

  GroupModel(this.members, this.icon);
}
